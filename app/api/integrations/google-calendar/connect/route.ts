import { randomBytes } from "node:crypto";import { cookies } from "next/headers";import { NextResponse } from "next/server";import { requireMasterAdmin,apiError } from "@/lib/google-calendar/route-auth";
export async function GET(){try{await requireMasterAdmin();const state=randomBytes(32).toString("base64url");(await cookies()).set("google_calendar_oauth_state",state,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:600});const url=new URL("https://accounts.google.com/o/oauth2/v2/auth");url.search=new URLSearchParams({client_id:process.env.GOOGLE_CALENDAR_CLIENT_ID||"",redirect_uri:process.env.GOOGLE_CALENDAR_REDIRECT_URI||"",response_type:"code",access_type:"offline",prompt:"consent",include_granted_scopes:"true",scope:"openid email https://www.googleapis.com/auth/calendar.events.owned https://www.googleapis.com/auth/calendar.calendarlist.readonly",state}).toString();return NextResponse.redirect(url)}catch(e){return apiError(e)}}

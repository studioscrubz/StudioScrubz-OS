import { calculatePublicRequest, loadAuthoritativeCatalog, PublicEstimateRequestError, type PublicRequest } from "@/lib/services/publicEstimateRequests";
export async function POST(request:Request){try{const input=await request.json() as PublicRequest;if(input.website)return Response.json({error:"Invalid request."},{status:400});const result=calculatePublicRequest(input,await loadAuthoritativeCatalog());return Response.json({price:result.finalPrice})}catch(error){return Response.json({error:error instanceof PublicEstimateRequestError?error.message:"Pricing could not be calculated."},{status:400})}}

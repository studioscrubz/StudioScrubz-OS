import type { Metadata } from "next";
import { EstimateCta, PageHero } from "@/components/site/SiteSections";
import { getPublicBusinessContact } from "@/lib/services/publicBusinessSettings";
export const metadata: Metadata = { title: { absolute: "Contact StudioScrubz | Los Angeles Cleaning Estimates" }, description: "Request a residential, commercial, post-construction, or pressure-washing estimate from StudioScrubz in Los Angeles County and the San Fernando Valley.", alternates: { canonical: "https://studioscrubz.com/contact" } };
export default async function ContactPage(){const contact=await getPublicBusinessContact();return <><PageHero eyebrow="Contact StudioScrubz" title="Let’s talk about your space." copy="For pricing and service requests, use the secure Estimate portal. For general questions, contact StudioScrubz directly."/><section className="px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto grid max-w-5xl gap-5 md:grid-cols-3"><ContactCard title="Request an Estimate"><p>Share your property and service details through the existing customer estimate experience.</p><EstimateCta className="mt-6"/></ContactCard><ContactCard title="Email">{contact.email?<a className="break-all font-extrabold text-[#143d1a]" href={`mailto:${contact.email}`}>{contact.email}</a>:<p>Business email is being updated.</p>}</ContactCard><ContactCard title="Call">{contact.phone?<a className="font-extrabold text-[#143d1a]" href={`tel:${contact.phone.replace(/[^+\d]/g,"")}`}>{contact.phone}</a>:<p>Business phone is being updated.</p>}</ContactCard></div><div className="mx-auto mt-10 max-w-5xl rounded-[1.6rem] bg-[#eef3ea] p-7 sm:p-10"><h2 className="text-2xl font-extrabold text-[#143d1a]">Service area</h2><p className="mt-3 max-w-4xl leading-7 text-neutral-600">StudioScrubz serves Los Angeles County with a strong focus on the San Fernando Valley, including Winnetka, Canoga Park, Woodland Hills, Reseda, Chatsworth, Northridge, Van Nuys, Encino, and Tarzana. Availability extends to surrounding Los Angeles and High Desert service areas based on the property, service, and schedule.</p></div></section></>}
function ContactCard({title,children}:{title:string;children:React.ReactNode}){return <article className="min-h-56 rounded-[1.6rem] border border-[#143d1a]/10 bg-white p-7 shadow-[0_16px_45px_rgba(20,61,26,.06)]"><h2 className="text-xl font-extrabold text-[#143d1a]">{title}</h2><div className="mt-4 text-sm leading-6 text-neutral-600">{children}</div></article>}

"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
import { createJobFromProposal,getJobForProposal } from "@/lib/services/jobs";
import type { JobWithRelations } from "@/types/job";

export function ProposalJobAction({proposalId}:{proposalId:string}){const[job,setJob]=useState<JobWithRelations|null>(null);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[failed,setFailed]=useState(false);const[notice,setNotice]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);useEffect(()=>{let active=true;void getJobForProposal(proposalId).then(value=>{if(active)setJob(value)}).catch((x:unknown)=>{console.error("Job relationship load failed",x);if(active)setError("Unable to check Job status.")}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[proposalId]);async function create(){setSaving(true);setFailed(false);setError(null);try{const created=await createJobFromProposal(proposalId);setJob(await getJobForProposal(proposalId)??created);setNotice("Job created successfully.")}catch(x){console.error("Job creation failed",x);setFailed(true);setError("Unable to create Job.")}finally{setSaving(false)}}if(loading)return <span className="rounded border px-2 py-1 text-[10px] font-bold text-neutral-400">Checking Job…</span>;if(job)return <><span className="rounded bg-[#edf4ec] px-2 py-1 text-[10px] font-bold text-[#143d1a]">Job Created</span><Link href={`/jobs?jobId=${job.id}`} className="rounded border px-2 py-1 text-[10px] font-bold text-[#143d1a]">View Job</Link>{notice&&<span className="w-full text-xs font-bold text-green-700">{notice}</span>}</>;return <><button type="button" disabled={saving} onClick={()=>void create()} className="rounded bg-[#143d1a] px-2 py-1 text-[10px] font-bold text-white disabled:opacity-50">{saving?"Creating Job…":failed?"Retry Job Creation":"Create Job"}</button>{error&&<span className="w-full text-xs font-bold text-red-700">{error}</span>}</>}

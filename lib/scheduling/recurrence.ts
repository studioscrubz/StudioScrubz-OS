import type{AgreementFrequency,RecurrenceRule}from"@/types/agreement";
export function generateServiceDates(start:string,end:string,frequency:AgreementFrequency,rule:RecurrenceRule,agreementEnd:string|null,autoRenew:boolean){const out:string[]=[],first=parse(start),last=parse(end),hardEnd=agreementEnd&&!autoRenew?parse(agreementEnd):last;for(let d=new Date(first);d<=last&&d<=hardEnd;d.setDate(d.getDate()+1)){const diff=Math.floor((d.getTime()-first.getTime())/864e5),weeks=Math.floor(diff/7),day=d.getDay(),dom=d.getDate();let match=false;if(frequency==="One-Time")match=diff===0;else if(frequency==="Daily")match=true;else if(frequency==="Weekly")match=rule.daysOfWeek.length?rule.daysOfWeek.includes(day as never):diff%7===0;else if(frequency==="Biweekly")match=rule.daysOfWeek.length?rule.daysOfWeek.includes(day as never)&&weeks%2===0:diff%14===0;else if(frequency==="Twice Monthly")match=dom===rule.dayOfMonth||dom===rule.secondDayOfMonth;else if(frequency==="Every 4 Weeks")match=rule.daysOfWeek.length?rule.daysOfWeek.includes(day as never)&&weeks%4===0:diff%28===0;else if(frequency==="Monthly")match=dom===(rule.dayOfMonth??first.getDate());else if(frequency==="Multiple Days Per Week")match=rule.daysOfWeek.includes(day as never)&&weeks%Math.max(1,rule.intervalWeeks)===0;else if(frequency==="Custom")match=diff%Math.max(1,rule.customIntervalDays??7)===0;if(match)out.push(local(d))}return out}function parse(x:string){return new Date(`${x}T12:00:00`)}function local(d:Date){return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}

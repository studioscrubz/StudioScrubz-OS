import { getSupabaseClient } from "@/lib/supabase/client";
import type { ChangeRequestDecision, PublicChangeRequest } from "@/types/changeRequest";

export async function getPublicChangeRequest(token:string):Promise<PublicChangeRequest>{if(!token)throw new Error("This Change Request link is invalid.");const{data,error}=await getSupabaseClient().rpc("get_change_request_by_token",{p_token:token});if(error)throw new Error(error.message||"This Change Request could not be loaded.");if(!data)throw new Error("This Change Request is invalid or not available.");return data}
export async function decidePublicChangeRequest(token:string,clientName:string,consent:boolean,decision:ChangeRequestDecision):Promise<PublicChangeRequest>{const name=clientName.trim();if(name.length<2)throw new Error("Enter your full name.");if(!consent)throw new Error("You must confirm your review before deciding.");const response=await fetch("/api/public/change-requests/decision",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,clientName:name,consent,decision})});const result=await response.json() as PublicChangeRequest&{error?:string};if(!response.ok)throw new Error(result.error||"The Change Request decision could not be saved.");return result}
